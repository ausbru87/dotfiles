# ~/.inputrc - Readline configuration

# Enable case-insensitive completion
set completion-ignore-case on

# Show all completions on first tab
set show-all-if-ambiguous on

# Show completion type with color
set colored-stats on

# Add trailing slash to directories
set mark-directories on
set mark-symlinked-directories on

# Don't ring bell on completion
set bell-style none

# Show common prefix before cycling
set menu-complete-display-prefix on

# Enable visible stats (show file type indicators)
set visible-stats on

# Vi mode (uncomment if you prefer vi keybindings)
# set editing-mode vi

# History search with up/down arrows
"\e[A": history-search-backward
"\e[B": history-search-forward

# Ctrl+Left/Right to move by word
"\e[1;5C": forward-word
"\e[1;5D": backward-word

# Ctrl+Backspace to delete word
"\C-h": backward-kill-word

# Clear screen with Ctrl+L
"\C-l": clear-screen

# Auto-complete from history with Ctrl+R (reverse search)
"\C-r": reverse-search-history
"\C-s": forward-search-history
